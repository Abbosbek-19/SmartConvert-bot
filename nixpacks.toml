[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = ["cd TelegramConverterBot && dotnet publish -c Release -o out"]

[start]
cmd = "cd TelegramConverterBot && dotnet out/TelegramConverterBot.dll"
